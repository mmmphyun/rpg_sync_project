
services:
  rpg-sync-app:
    build: .
    container_name: rpg-sync-container
    restart: always
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - ./public/images:/app/public/images